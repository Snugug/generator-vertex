//////////////////////////////
  // Callback for `<%= name %>` <%= type %>
  //////////////////////////////
  function <%= funcName %> () {
    return {
      restrict: 'EA',
      link: function ($scope, $element, $attrs) {
        var el = element[0];
      }
    }
  }
